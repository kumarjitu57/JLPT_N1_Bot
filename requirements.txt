# Telegram bot
python-telegram-bot==20.7

# Flask for Render hosting
Flask==2.3.5

# Google Gemini AI client
google-generativeai==0.1.4

# Optional: for async support (usually included in Python 3.9+)
aiohttp==3.8.5

# Optional: for text-to-speech if you use gTTS
gTTS==2.3.2

# Optional: SQLite is built-in, no need to install
