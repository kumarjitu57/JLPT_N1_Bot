services:
  - type: web
    name: jlpt-n1-bot
    env: python
    plan: free
    region: oregon        # or choose your preferred region
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GEMINI_API_KEY
        value: "YOUR_GEMINI_API_KEY"    # Replace with your actual Gemini API key
      - key: TELEGRAM_TOKEN
        value: "YOUR_TELEGRAM_BOT_TOKEN" # Replace with your actual Telegram bot token
